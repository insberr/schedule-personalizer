import { intervalToDuration, formatDuration, isAfter } from 'https://cdn.skypack.dev/date-fns' //TODO pin this so it doesnt change
window.intervalToDuration = intervalToDuration
window.formatDuration = formatDuration
window.isAfter = isAfter